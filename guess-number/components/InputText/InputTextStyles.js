import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textInput: {
    height: 40,
    borderColor: "black",
    borderWidth: 1,
    marginVertical: 10,
  },
});

export default styles;
